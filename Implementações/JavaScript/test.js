

function soma(s1,s2) {
	let resultado = s1 + s2;
	return resultado;
	}

//Solicita ai usuário a entrada de dois numeros
let s1 = Number(prompt("Digite um número: "));
let s2 = Number(prompt("Digite outro número: "));
let resultado = soma(s1,s2);

alert("A soma dos números é: "+ resultado);

